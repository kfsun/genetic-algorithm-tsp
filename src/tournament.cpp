#include "tournament.hpp"

Tournament::Tournament(size_t size) {
  size_ = size;
}

void Tournament::new_turn() {
}

/*
void Tournament::add_candidate(std::shared_ptr<Candidate> pcandidate) {
  if (candidates.size() < size_) {
    candidates.push_back(pcandidate);
  }
}
*/

